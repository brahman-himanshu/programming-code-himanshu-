
#include<iostream>
using namespace std;

void fun(int i , int n ,int sum ){
    if (i>n)
    {
        cout<<"sum "<< sum ;
    return ;   /* code */
    }

     fun(i + 1, n, sum + i);

    
}
int main (){
    int sum ,n;
    cout<< "enter the n number for the sum of n num " ;
    cin>>n;
fun(1,n,0);
}
